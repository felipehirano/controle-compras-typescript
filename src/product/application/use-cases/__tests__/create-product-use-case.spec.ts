import CreateProductUseCase from "../create-product-use-case";

describe('CreateProductUseCase Unit Tests', () => {
    it('should create a product entity', () => {
        expect(2).toBe(2);
        // const productCreate = new CreateProductUseCase();
    }) 
});